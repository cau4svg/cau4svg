# References for the Profile README

This file records decisions and dependencies used in the profile README.

## Current data

- GitHub username: `cau4svg`
- Name: `Cauã Alves`
- Country: Brazil
- Expected profile repository: `cau4svg/cau4svg`
- Main file: `README.md`

## External dependencies used in the README

- Shields.io for lightweight badges.
- skillicons.dev for stack icons.
- The repository's own `output` branch for the optional snake animation generated by workflow.

## Pending data

- Professional email, if it should be public.
- LinkedIn URL, if it should be public.
- Real repositories to highlight when they are ready.
- Final short bio, if the current one needs refinement.

## Notes

- GitHub does not execute JavaScript inside README files.
- `iframe`, `script` and external CSS should not be used.
- Local images live in `assets/`.
- The snake animation only appears after the workflow publishes the `output` branch.
- Unstable external stat cards are intentionally avoided in the README to prevent broken images.
